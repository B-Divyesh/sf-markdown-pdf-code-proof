[
  {"id":"single-line-wrap","claim":"A one-line code fence that wraps onto multiple PDF baselines fails the release check.","where":"README Checks; landing Missing or wrapped code","test":"cargo test --test cli wrapped_single_code_line_fails_the_release_contract -- --exact","sandbox":"Generated one-page PDF splits one source line across two baselines; assert exit 1, code.flow-changed, and HOLD proof."},
  {"id":"page-bounds","claim":"Text is checked against every visible PDF page edge.","where":"README Checks; landing Page bounds","test":"cargo test --test cli page_bounds_cover_every_media_and_crop_edge -- --exact","sandbox":"Generated PDFs place text beyond all four MediaBox edges and a non-default CropBox; every case exits 1."},
  {"id":"code-content","claim":"Missing code fence text is reported as a release defect.","where":"landing Missing or wrapped code","test":"cargo test --test cli missing_code_content_fails_the_release_contract -- --exact","sandbox":"Generated PDF omits a code fence line; assert exit 1 and code.content-missing."},
  {"id":"internal-links","claim":"A Markdown fragment cannot pass with a wrong PDF destination.","where":"README Checks; landing Internal links","test":"cargo test --test cli wrong_pdf_destination_cannot_satisfy_a_fragment -- --exact","sandbox":"Generated PDF maps a source fragment to an unrelated destination; assert exit 1 and link.destination-missing."},
  {"id":"syntax-color","claim":"Missing syntax color warns by default and fails when warnings are denied.","where":"README Checks; landing Syntax color","test":"cargo test --test cli missing_syntax_color_warns_and_respects_warning_policy -- --exact","sandbox":"Language-tagged fixture has no color operator; assert warning/exit 0 then exit 1 with --deny-warnings."},
  {"id":"existing-pdf","claim":"An existing PDF can be checked without starting a renderer.","where":"README Usage; landing workflow and safety","test":"cargo test --test cli documented_existing_pdf_flow_passes_and_writes_proof -- --exact","sandbox":"Fresh temporary Markdown/PDF pair is checked with --pdf; assert PASS JSON and an HTML proof sheet."},
  {"id":"local-cli-files","claim":"The CLI reads the supplied paths and writes HTML proof sheet artifacts locally.","where":"Privacy; metadata","test":"cargo test --test cli documented_existing_pdf_flow_passes_and_writes_proof -- --exact","sandbox":"Fresh temporary source/PDF paths are supplied to --pdf; assert the requested local proof/index.html is written."},
  {"id":"renderer-no-shell","claim":"Custom renderer placeholders become individual arguments, never shell input.","where":"README Usage; landing workflow","test":"cargo test --test cli custom_renderer_runs_without_a_shell_and_is_checked -- --exact","sandbox":"A temporary renderer receives substituted input/output arguments and produces a checked HTML proof sheet."},
  {"id":"renderer-network","claim":"Linux renderer subprocesses cannot create network connections.","where":"README Renderer safety; landing Renderer safety","test":"cargo test --test cli renderer_sandbox_denies_network_connections -- --exact","sandbox":"A sandboxed curl renderer targets a loopback listener; assert that listener receives no connection."},
  {"id":"renderer-fail-closed","claim":"A renderer does not start when Linux sandbox setup fails.","where":"README Renderer safety; landing Renderer safety","test":"cargo test --test cli renderer_refuses_to_start_when_sandbox_setup_fails -- --exact","sandbox":"Inject sandbox setup refusal before spawn; assert exit 2 and that a renderer marker file is absent."},
  {"id":"renderer-timeout","claim":"A renderer has a deadline set by --timeout.","where":"README Renderer safety; landing Renderer safety","test":"cargo test --test cli renderer_deadline_stops_a_long_running_command -- --exact","sandbox":"Run a two-second renderer with --timeout 1; assert exit 2 and deadline diagnostic."},
  {"id":"html-proof","claim":"A successful check writes an HTML proof sheet.","where":"README opening","test":"cargo test --test cli documented_existing_pdf_flow_passes_and_writes_proof -- --exact","sandbox":"A valid temporary source/PDF pair exits 0 and writes proof/index.html containing PASS."},
  {"id":"json-report","claim":"A check can write a JSON report for CI.","where":"README Usage; landing workflow","test":"cargo test --test cli json_report_and_exit_codes_are_observable -- --exact","sandbox":"A valid temporary source/PDF pair runs with --json; assert JSON passed:true output."},
  {"id":"exit-codes","claim":"Exit 0 means pass, exit 1 means defects, and exit 2 means the check could not finish.","where":"README Usage; landing workflow","test":"cargo test --test cli json_report_and_exit_codes_are_observable -- --exact","sandbox":"Valid, defect, and missing-source fixtures assert 0, 1, and 2 respectively."},
  {"id":"sample-demo","claim":"The bundled sample runs without user files or a renderer and writes a HOLD HTML proof sheet.","where":"README sample; landing primary action","test":"cargo test --test cli demo_uses_bundled_sample_data_and_writes_an_isolated_proof -- --exact","sandbox":"Run codeproof demo into a fresh temporary directory; assert bundled sample, generated PDF, code.flow-changed, and HOLD HTML proof sheet."},
  {"id":"private-site","claim":"The documentation demo makes only same-origin requests and stores no tracking data.","where":"landing facts; Privacy","test":"npm run test:site -- --grep @claim:private-site","sandbox":"Fresh browser context records request origins, cookies, localStorage, and sessionStorage through the demo flow."},
  {"id":"offline-reload","claim":"The installed documentation shell works offline after the first visit.","where":"offline status; Privacy","test":"npm run test:site -- --grep @claim:offline-reload","sandbox":"Dedicated browser context waits for the active service worker, goes offline, reloads, and asserts docs plus offline status."},
  {"id":"rust-msrv","claim":"The package and locked dependencies compile with Rust 1.88.","where":"README Install; landing Install","test":"npm run test:msrv","sandbox":"Read locked dependency metadata then run cargo +1.88 check --workspace --locked."},
  {"id":"install-from-git","claim":"The published install command works from an empty directory.","where":"README Install; landing install command","test":"npm run test:install","sandbox":"Cargo installs codeproof from the public Git repository into a fresh temporary root, then runs --version."},
  {"id":"mit-license","claim":"Code Proof is published under the MIT License.","where":"landing facts; Terms; README","test":"npm run test:license","sandbox":"Assert MIT text in LICENSE, MIT crate metadata, and license references in the landing page and Terms."}
]
